@model AA.HRMS.Interfaces.ILeaveRequestViewModel

@{
    ViewBag.Title = "Request for Leave";
}

<div class="modal-header">
    <h3>@ViewBag.Title</h3>
    <button type="button" class="close" data-dismiss="modal">&times;</button>
</div>



@using (Html.BeginForm("CreateLeaveRequest", "Leave", FormMethod.Post, new { id = "RequestforLeaveForm" }))
{

<div class="modal-body">

    <input type="hidden" value="@Model.EmployeeID" id="EmployeeID" name="EmployeeID" />
    <input type="hidden" value="@Model.CompanyId" id="CompanyId" name="CompanyId" />
    <input type="hidden" value="@Model.URL" id="url" name="url" />

    @if (!string.IsNullOrEmpty(Model.ProcessingMessage))
    {

        <div id="alertMsg" class="alert alert-info alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true"><i class="notika-icon notika-close"></i></span></button> @Model.ProcessingMessage
        </div>
    }

    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-danger alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true"><i class="notika-icon notika-close"></i></span></button> @Html.ValidationSummary()
        </div>
    }


    @Html.AntiForgeryToken()

    <div class="form-example-int">
        <div class="form-group">
            <label>Leave Type</label>
            <div class="">
                @Html.DropDownList("LeaveTypeID", Model.LeaveTypeDropDown, new { @class = "form-control", id = "LeaveTypeID" })
            </div>
        </div>
    </div>

    <div class="form-example-int">
        <div class="form-group">
            <label>Date Leave Starts</label>
            <div class="">
                <input id="DateLeaveStart" name="DateLeaveStart" type="date" class="form-control" value="@Model.DateLeaveStart" />
            </div>
        </div>
    </div>


    <div class="form-example-int">
        <div class="form-group">
            <label>Date Leave ends</label>
            <div class="">
                <input id="DateLeaveEnds" data-annualLeaveDuration="@Model.AnnualLeaveDuration" name="DateLeaveEnds" type="date" class="form-control" value="@Model.DateLeaveEnds" />
            </div>
        </div>
    </div>

    <div class="form-example-int">
        <div class="form-group">
            <label>Comment</label>
            <div class="">
                <textarea id="Comment" name="Comment" type="text" class="form-control" value="@Model.Comment">@Model.Comment</textarea>
            </div>
        </div>
    </div>

</div>
           
  <div class="modal-footer">
    <button type="submit" name="submitButton" value="Submit" class="btn btn-default">Request</button>
    <button type="button" class="btn btn-sm btn-danger" data-dismiss="modal">Close</button>
  </div>

}
                       
